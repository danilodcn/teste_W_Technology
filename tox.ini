[tox]
envlist = clean,lint,stats,clean
skipsdist = True

[testenv:clean]
deps =
    coverage

commands =
    coverage erase


[testenv:lint]
deps =
    pipenv
    coverage
    pylava

commands = 
    pipenv install
    coverage run --source='core' manage.py test
    pylava core
    ; pylava w_technology


[testenv:stats]
deps =
    coverage

commands = 
    coverage report
    coverage html